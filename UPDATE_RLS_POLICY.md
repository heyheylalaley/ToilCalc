# Обновление RLS политики для редактирования имени

## Что изменилось

Теперь пользователи могут редактировать свое имя, а админы могут удалять пользователей.

## Что нужно сделать в Supabase

### Шаг 1: Обновите политику RLS для таблицы users

1. Откройте Supabase Dashboard: https://supabase.com/dashboard
2. Выберите проект
3. Перейдите в **SQL Editor**
4. Выполните следующий SQL:

```sql
-- Удаляем старую политику
DROP POLICY IF EXISTS "Admins can update users" ON users;

-- Пользователи могут обновлять свое имя
CREATE POLICY "Users can update own name" ON users
  FOR UPDATE USING (
    LOWER(TRIM(email)) = LOWER(TRIM(auth.jwt() ->> 'email'))
  )
  WITH CHECK (
    LOWER(TRIM(email)) = LOWER(TRIM(auth.jwt() ->> 'email'))
  );

-- Админы могут обновлять всех пользователей
CREATE POLICY "Admins can update users" ON users
  FOR UPDATE USING (public.is_admin());
```

### Шаг 2: Проверьте результат

1. Выполните в SQL Editor:

```sql
-- Проверяем политики для users
SELECT schemaname, tablename, policyname 
FROM pg_policies 
WHERE tablename = 'users'
ORDER BY policyname;
```

2. Должны быть следующие политики:
   - `Anyone can read users` (SELECT)
   - `Anyone can insert users` (INSERT)
   - `Users can update own name` (UPDATE)
   - `Admins can update users` (UPDATE)
   - `Admins can delete users` (DELETE)

## Что теперь работает

1. **Пользователи могут редактировать свое имя**:
   - В user view есть кнопка "Edit Name"
   - При нажатии открывается модальное окно
   - Пользователь может изменить только свое имя

2. **Админы могут удалять пользователей**:
   - В списке пользователей (admin view) есть кнопка удаления (🗑️) для каждого пользователя
   - При удалении пользователя автоматически удаляются все его логи (CASCADE)
   - Админ не может удалить свой собственный аккаунт

3. **Мультипликатор теперь только для чтения**:
   - Мультипликатор всегда 1.5 из базы данных
   - Кнопка изменения мультипликатора удалена
   - Мультипликатор отображается только для информации

## Важно

- После применения изменений пользователи смогут редактировать только свое имя (поле `name`)
- Админы могут редактировать любые поля пользователей
- При удалении пользователя все его логи удаляются автоматически (ON DELETE CASCADE)

